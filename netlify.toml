[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # Extend timeout to 26 seconds (max on free tier)
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
  force = true
